[tool.black]
line-length = 100

[tool.ruff]
line-length = 100
select = ["E","F","I","UP"]
ignore = ["E501"]

[tool.pytest.ini_options]
addopts = "-q --cov=diabetes_app --cov-report=term-missing"
testpaths = ["tests"]
